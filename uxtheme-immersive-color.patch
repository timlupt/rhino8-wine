--- a/dlls/uxtheme/uxtheme.spec	2026-06-18 01:48:20.420174500 +0000
+++ b/dlls/uxtheme/uxtheme.spec	2026-06-18 01:48:47.750173019 +0000
@@ -43,6 +43,10 @@
 61 stdcall OpenThemeDataEx(ptr wstr long)
 62 stub -noname ServerClearStockObjects
 63 stub -noname MarkSelection
+95 stdcall GetImmersiveColorFromColorSetEx(long long long long)
+96 stdcall -noname GetImmersiveColorTypeFromName(wstr)
+98 stdcall GetImmersiveUserColorSetPreference(long long)
+100 stdcall -noname GetImmersiveColorNamedTypeByIndex(long)
 104 stdcall -noname RefreshImmersiveColorPolicyState()
 132 stdcall -noname ShouldAppsUseDarkMode()
 133 stdcall -noname AllowDarkModeForWindow(ptr long)
--- a/dlls/uxtheme/system.c	2026-06-18 01:48:20.424174499 +0000
+++ b/dlls/uxtheme/system.c	2026-06-18 01:49:10.486171787 +0000
@@ -1334,3 +1334,62 @@
     FIXME("%p: stub\n", hwnd);
     return FALSE;
 }
+
+/**********************************************************************
+ *      GetImmersiveColorFromColorSetEx                    (UXTHEME.95)
+ *
+ * Returns the immersive colour for (color_set, color_type) as 0xAABBGGRR.
+ * Undocumented. The full per-type colour tables are not implemented; as a
+ * first approximation, follow AppsUseLightTheme so callers that inspect a
+ * foreground colour (e.g. application text) get a value consistent with the
+ * active light/dark theme: opaque black for light mode, opaque white for dark.
+ */
+DWORD WINAPI GetImmersiveColorFromColorSetEx(UINT color_set, UINT color_type,
+                                             BOOL ignore_high_contrast, UINT high_contrast_cache_mode)
+{
+    DWORD light_theme = TRUE, light_theme_size = sizeof(light_theme);
+
+    FIXME("(%u, %u, %d, %u): semi-stub\n", color_set, color_type,
+          ignore_high_contrast, high_contrast_cache_mode);
+
+    RegGetValueW(HKEY_CURRENT_USER,
+                 L"Software\\Microsoft\\Windows\\CurrentVersion\\Themes\\Personalize",
+                 L"AppsUseLightTheme", RRF_RT_REG_DWORD, NULL, &light_theme, &light_theme_size);
+
+    return light_theme ? 0xff000000 : 0xffffffff;
+}
+
+/**********************************************************************
+ *      GetImmersiveColorTypeFromName                      (UXTHEME.96)
+ *
+ * Maps an immersive colour name (e.g. L"ImmersiveApplicationText") to its
+ * colour-type index. Undocumented; exported by ordinal only.
+ */
+int WINAPI GetImmersiveColorTypeFromName(const WCHAR *name)
+{
+    FIXME("%s: stub\n", debugstr_w(name));
+    return 0;
+}
+
+/**********************************************************************
+ *      GetImmersiveUserColorSetPreference                 (UXTHEME.98)
+ *
+ * Index of the user's active immersive colour set. Undocumented.
+ */
+int WINAPI GetImmersiveUserColorSetPreference(BOOL force_check_registry, BOOL skip_check_on_fail)
+{
+    FIXME("%d, %d: stub\n", force_check_registry, skip_check_on_fail);
+    return 0;
+}
+
+/**********************************************************************
+ *      GetImmersiveColorNamedTypeByIndex                  (UXTHEME.100)
+ *
+ * Returns a pointer to the colour-type name structure for the given index.
+ * Undocumented; exported by ordinal only.
+ */
+const void * WINAPI GetImmersiveColorNamedTypeByIndex(UINT index)
+{
+    FIXME("%u: stub\n", index);
+    return NULL;
+}
--- a/dlls/uxtheme/tests/system.c	2026-06-18 01:48:20.427174499 +0000
+++ b/dlls/uxtheme/tests/system.c	2026-06-18 01:51:08.994165367 +0000
@@ -52,6 +52,10 @@
 static HRESULT (WINAPI *pGetThemeTransitionDuration)(HTHEME, int, int, int, int, DWORD *);
 static BOOLEAN (WINAPI *pShouldSystemUseDarkMode)(void);
 static BOOLEAN (WINAPI *pShouldAppsUseDarkMode)(void);
+static DWORD (WINAPI *pGetImmersiveColorFromColorSetEx)(UINT, UINT, BOOL, UINT);
+static int (WINAPI *pGetImmersiveColorTypeFromName)(const WCHAR *);
+static int (WINAPI *pGetImmersiveUserColorSetPreference)(BOOL, BOOL);
+static const void * (WINAPI *pGetImmersiveColorNamedTypeByIndex)(UINT);
 
 static LONG (WINAPI *pDisplayConfigGetDeviceInfo)(DISPLAYCONFIG_DEVICE_INFO_HEADER *);
 static LONG (WINAPI *pDisplayConfigSetDeviceInfo)(DISPLAYCONFIG_DEVICE_INFO_HEADER *);
@@ -82,6 +86,10 @@
     pCloseThemeFile = (void *)GetProcAddress(uxtheme, MAKEINTRESOURCEA(3));
     pShouldSystemUseDarkMode = (void *)GetProcAddress(uxtheme, MAKEINTRESOURCEA(138));
     pShouldAppsUseDarkMode = (void *)GetProcAddress(uxtheme, MAKEINTRESOURCEA(132));
+    pGetImmersiveColorFromColorSetEx = (void *)GetProcAddress(uxtheme, "GetImmersiveColorFromColorSetEx");
+    pGetImmersiveColorTypeFromName = (void *)GetProcAddress(uxtheme, MAKEINTRESOURCEA(96));
+    pGetImmersiveUserColorSetPreference = (void *)GetProcAddress(uxtheme, "GetImmersiveUserColorSetPreference");
+    pGetImmersiveColorNamedTypeByIndex = (void *)GetProcAddress(uxtheme, MAKEINTRESOURCEA(100));
 
 #define GET_PROC(module, func)                       \
     p##func = (void *)GetProcAddress(module, #func); \
@@ -2778,6 +2786,29 @@
     ok(result == !light_theme, "Expected value %d, got %d\n", !light_theme, result);
 }
 
+static void test_GetImmersiveColors(void)
+{
+    int type, pref;
+
+    if (!pGetImmersiveColorFromColorSetEx || !pGetImmersiveColorTypeFromName
+        || !pGetImmersiveUserColorSetPreference || !pGetImmersiveColorNamedTypeByIndex)
+    {
+        win_skip("Immersive colour set functions are unavailable.\n");
+        return;
+    }
+
+    pref = pGetImmersiveUserColorSetPreference(FALSE, FALSE);
+    ok(pref >= 0, "Unexpected active colour set %d.\n", pref);
+
+    type = pGetImmersiveColorTypeFromName(L"ImmersiveApplicationText");
+    ok(type >= 0, "Unexpected colour type %d.\n", type);
+
+    /* The exact colour and the named-type pointer are theme/version dependent,
+     * so just exercise these to confirm the exports are callable. */
+    pGetImmersiveColorFromColorSetEx(0, type >= 0 ? type : 0, FALSE, 0);
+    pGetImmersiveColorNamedTypeByIndex(0);
+}
+
 static void test_DrawThemeEdge(void)
 {
     HTHEME htheme;
@@ -2883,6 +2914,7 @@
     test_theme(FALSE);
     test_ShouldSystemUseDarkMode();
     test_ShouldAppsUseDarkMode();
+    test_GetImmersiveColors();
     test_DrawThemeEdge();
     test_DrawThemeTextEx();
 
